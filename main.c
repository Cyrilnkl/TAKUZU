#include "functions.h"
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(){
    Menu_principal(0);
}


/*do{
printf("CP");
display_pad(L, size+2);
printf("cp2");
invent_pad(L);
update(L, size);
fill_pad(L, size);
update(L, size);
k++;
}while (plagiat(L, size)!=0);*/