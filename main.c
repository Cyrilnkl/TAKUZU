#include "functions.h"
#include <stdio.h>
#include <stdlib.h>

int main(){
    Start_game();
}


/*do{
printf("CP");
display_pad(L, size+2);
printf("cp2");
invent_pad(L);
update(L, size);
fill_pad(L, size);
update(L, size);
k++;
}while (plagiat(L, size)!=0);*/