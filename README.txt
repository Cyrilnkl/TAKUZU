Cyril NAKHLA and Eyfeline TALA Promo 2026, L1-Int4.

The file to execute is main.c 
From there just Follow instructions.

The program can:
-Play a grid
-Generate a grid
-Solve a grid automatically
-Create a mask automatically
-Allow to enter a mask manually